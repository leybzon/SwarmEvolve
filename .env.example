# Copy to .env and fill in. .env is git-ignored.
# Keys are read only by scripts/llm_client.py.

ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# Optional: override sandbox container image tag
SWARMEVOLVE_SANDBOX_IMAGE=swarmevolve-sandbox:latest
